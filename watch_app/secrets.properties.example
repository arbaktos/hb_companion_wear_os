# Copy to secrets.properties (gitignored) and fill in real values.
# Real values live in ../LOCAL_NOTES.md (also gitignored).
BASE_URL=https://<vm-ip-dashed>.nip.io:8001
BEARER_TOKEN=<openssl rand -hex 32 value from /etc/baby-svc.env on the VM>
